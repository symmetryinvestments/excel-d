language: d
sudo: false
dist: bionic

install:
  - mkdir bin

matrix:
  include:
    - d: dmd-nightly
    - d: dmd-2.091.0
    - d: dmd-2.088.1
    - d: ldc-1.20.1
    - d: ldc-1.17.0
  allow_failures:
    - d: dmd-nightly

script:
  - dub test --build=unittest-cov --compiler=${DC}

after_success:
 - bash <(curl -s https://codecov.io/bash)
